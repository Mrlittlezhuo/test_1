# 从论文复现到研究路线选择：Codex 与 Git 如何协同做科研

> 大组会汇报素材。本文关注科研推进中反复出现的真实问题，以及我们如何用 Codex 和 Git 管理算法尝试；不是 Codex 功能清单，也不是 Git 命令教程。

## 一、科研推进不是一条直线

做研究时，我们并不是先确定一个算法，再把它写完。更常见的过程是：

1. 遇到一个具体问题，例如反射面数量未知、反射点分组不可靠，或某一步估计误差过大。
2. 阅读不同论文，寻找能解决这个问题的模型或算法。
3. 先按论文的条件复现，确认我们理解的公式、实现和结果一致。
4. 把算法放进当前研究场景，检查它需要的观测、先验和假设是否真的具备。
5. 如果不适配，就改造、替换，或停止这条路线；如果有效，再与现有算法融合并继续验证。

真正困难的不是“找到了一个看起来很好的公式”，而是弄清楚：**这篇论文的方法为什么有效，它依赖什么条件，以及这些条件在我们的系统里是否成立。**

例如，论文可能默认有导频、已知反射面数量、准确的反射点或独立同分布噪声；我们的研究可能不满足这些条件。原算法即使复现成功，也不能直接宣称适用于当前课题。

## 二、这一过程中会遇到什么问题

| 科研环节 | 经常遇到的问题 | 我们需要留下的证据 |
|---|---|---|
| 阅读论文 | 多篇论文符号不同、假设不一致，方法看似相同但输入条件不同 | 来源、核心假设、输入输出、适用范围 |
| 算法复现 | 公式省略实现细节，代码能运行却复现不了论文结果 | 独立复现代码、配置、对照指标、差异说明 |
| 接入项目 | 原方法要求当前系统没有的观测，或和已有模块的数据定义冲突 | 接口映射、额外假设、需要改动的位置 |
| 算法改造 | 一处改动可能改善局部残差，却恶化最终科研目标 | 原方法与改造方法的同条件对照 |
| 多路线探索 | 同时试验几种方法容易互相覆盖，后来分不清结果来自哪一版代码 | 独立分支、明确提交、实验记录 |
| 失败与回退 | 失败算法仍有信息价值，不能简单覆盖或“假装没试过” | 失败原因、边界条件、可恢复的代码版本 |

因此，我们需要管理的不只是代码，还包括**论文依据、算法假设、讨论过程、实验配置、负结果和研究决策**。

## 三、Codex 在这条科研链中做什么

### 1. 阅读与拆解论文

我们让 Codex 帮助梳理论文的研究问题、观测模型、变量、目标函数、推导链和适用条件，再和现有代码中的输入逐项核对。

这一步的价值是快速暴露“不适配”：例如某方法依赖导频或真值标签，而我们的系统在实际运行时拿不到这些信息。**读懂论文与能在本项目中使用，是两道不同的关。**

### 2. 独立复现与验证

对有希望的方法，先在相对独立的实现里复现最小实验，检查公式、数值行为和原文结论。验证时保留论文条件下的设置，避免尚未复现成功就同时加入项目自己的改造。

“代码跑通”只是第一层；还要解释为什么输出与论文一致或不一致。对不上时，Codex 可以协助定位符号约定、坐标系、归一化、初始化、评价指标等差异，但不能凭猜测把结果写成“复现成功”。

### 3. 与当前研究融合

复现后再回答三个问题：

- **能不能用：**所需输入是否可观测，物理假设是否符合当前场景？
- **怎么接：**输入输出如何对应现有 AoA、反射参数、反射点和平面估计流程？
- **值不值得用：**它是否改善我们真正关心的指标，而不仅是某个内部代价？

如果答案不理想，Codex 协助提出可解释的改造方案、推导新目标函数、实现对照实验；研究者决定是否接受这些新假设。

### 4. 让讨论变成可重复的成果

每次探索至少沉淀四类内容：算法说明、可运行代码、实验记录、版本标识。聊天可以促进思考，但不应成为唯一的知识存放处；关键判断必须进入文档和 Git 历史。

## 四、重点：Codex 的聊天分叉如何与 Git 分支配合

这两种“分支”解决的问题不同：

| 层次 | 分叉后保留什么 | 主要用途 |
|---|---|---|
| Codex 聊天分叉 | 之前的讨论、问题背景和推理上下文 | 从同一个研究分歧点尝试不同思路 |
| Git 分支 | 某个提交之后的代码和文档变化 | 隔离不同算法实现，比较、保留或回退 |

**聊天分叉不等于 Git 分支自动建立。**Codex 可以通过 `/fork` 等方式保留当前讨论脉络并尝试另一种思路，见 [OpenAI 官方文档：斜杠命令](https://learn.chatgpt.com/docs/reference/slash-commands)；但代码要隔离，仍需明确创建或切换 Git 分支。不能把“讨论分叉”误认为“代码已经隔离”。

我们的理想做法是让两层分支围绕同一个研究问题对应起来：

```text
共同的科研问题与稳定基线
        ├─ 聊天 A：直接采用论文方法
        │      └─ Git 分支 A：原算法复现及接入
        └─ 聊天 B：修改论文假设或目标函数
               └─ Git 分支 B：改造算法及对照实验
```

聊天保留“为什么这样想”，Git 保留“当时实际运行了什么”。实验记录则把它们连到“结果是什么”。这种对应关系需要我们主动命名和记录，不能假设产品会自动替研究者完成科研判断。

### 一次算法分叉怎样落地

1. **固定起点。**先把当前可靠基线提交，记下起始提交；不在一堆未提交改动上直接比较两个方法。
2. **分叉讨论。**在同一问题节点创建不同 Codex 聊天，各自只讨论一条算法路线。
3. **隔离实现。**为每条路线建立独立 Git 分支。每次让 Codex 修改代码前确认当前分支，切换路线前先提交或妥善保留现有修改，避免 A 的试验混入 B。
4. **小步提交。**分别记录“论文复现”“接入项目”“算法改造”“实验结果”等可解释变化。
5. **同条件比较。**固定数据、随机种子、输入和评价指标，重点比较最终科研目标。
6. **做出选择。**合适的路线合并到稳定主线；暂不合适的路线记录失败原因后停止或归档。

Codex 可以协助执行创建分支、检查差异、提交、推送、合并和整理记录等重复性 Git 操作；**是否合并、是否放弃算法仍由研究者依据证据决定**。所谓“自动化管理”，是流程操作的自动化和版本可追溯，不是让 AI 自动宣布哪条科研路线正确。

### 删除“不合适的算法”究竟意味着什么

不合适的算法可以从当前主流程移除，实验分支也可以在整理后删除；但删除前应保留其论文来源、关键提交、实验记录和失败原因。尤其是尚未合并的分支，如果没有可访问的提交或归档引用，直接删除会让后续追溯困难。

科研中的“放弃”最好表示：**不再作为当前候选方法，而不是抹掉曾经验证过什么。**

## 五、Paper2 项目中的实际例子

### 例子一：第三阶段全局联合优化

我们曾尝试把 AoA、DDOA、共面和镜面反射相关约束纳入全局优化。实现后发现，某些残差即便下降，最终的基站定位误差也未必同步改善；数值求解还出现过病态问题。

这让我们没有继续无条件叠加复杂约束，而是暂时退出该路线，重新检查误差来源和研究目标。Codex 帮助定位实现与理论问题、整理结果；Git 保留该探索与之前的稳定代码，使路线调整不需要抹掉历史。

这个案例说明：**算法局部指标变好，不等于科研目标变好。失败路线也能帮助重新定义问题。**

### 例子二：多反射面估计

研究重心转向多个反射面后，我们先建立可比较的基础方法，再逐步探索硬共面 AoA 拟合、时序分段、动态规划和未知反射面数量选择。每推进一步，都要区分是“估计器本身更准”“边界找得更准”，还是“模型数量选得更准”。

后来又发现完整区间搜索的计算量较大，于是研究粗糙度引导的受限搜索。新路线在独立分支上开发，与完整全局动态规划保持同场景对照；若加速方案不可靠，原基线仍可继续使用。

这个案例说明：**Git 分支不是为“多写几份代码”，而是让新想法能够和可信基线公平比较，成功再融合。**

> 以上是本项目算法路线的复盘，不意味着每个方法都已完成逐篇论文的严格复现；正式汇报某篇论文的复现结果时，还应单独给出论文、参数设置、指标和复现差异。

## 六、这套协同方式带来的核心价值

第一，**降低试错成本**。想法可以大胆分叉，但每条路线都从明确基线出发，失败后不必重建旧版本。

第二，**让结果可解释**。看到一张图或一个误差数字时，可以追溯到论文假设、算法改动、实验配置和 Git 提交，而不是只记得“某次运行好像更好”。

第三，**让负结果有价值**。不合适的算法留下边界条件与失败证据，避免几个月后再次重复同一尝试。

第四，**让长期研究可接续**。新的 Codex 聊天可以从文档、分支和提交恢复上下文；研究者不需要把所有决策都留在脑中或某个超长聊天里。

## 七、适合汇报时讲的一句话

> 我们把 Codex 用作论文理解、算法复现、改造实现和实验整理的协作助手；把 Git 用作不同研究假设的独立试验场和证据链。聊天分叉保留思路，Git 分支隔离代码，实验记录连接结果。合适的算法进入主线，不合适的路线留下结论后退出，科研推进因此可比较、可回退、可复现。
