# 长对话科研：把关键推导和实验结论写进文档

> 大组会汇报内容稿，建议做成 3 页 PPT。本轮只修订文档与页面构思，不修改演示文稿。

## 一、这条主线为什么成立

科研任务往往会在同一段聊天中持续很久：从阅读论文、推导算法，到修改代码、分析实验，再回头调整假设。对话越来越长，但 Codex 当前能够直接利用的上下文有边界。长对话可以通过上下文压缩继续进行。官方说明，压缩会用更少的内容保留后续工作所需的**关键状态**；它并不等于逐字保留此前所有消息。[OpenAI Docs：上下文压缩](https://developers.openai.com/api/docs/guides/compaction)、[Codex 对话压缩机制](https://learn.chatgpt.com/docs/app-server)

由此产生一个实际风险：早期推导中的某个前提、一次实验的具体设置，或当时为什么否定某条路线，之后可能没有被准确带入。**这不是说压缩必然出错，而是不能把长聊天当作唯一的科研档案。**

你的主线正好解决这个问题的关键部分：讨论可以留在聊天里推进，但经过核对的推导、实验结果和路线判断要及时写入项目文档。以后重新讨论同一问题时，让 Codex 先读取对应文档，再结合当前代码和实验数据回答。

## 二、哪些内容必须从聊天中沉淀出来

| 文档内容 | 至少写清楚什么 | 后续能回答的问题 |
|---|---|---|
| 算法推导 | 研究问题、符号定义、模型假设、关键推导、适用条件 | 这个公式从哪里来？在当前场景下还能用吗？ |
| 实验记录 | 数据与配置、随机种子、评价指标、结果、异常现象、对应 Git 提交 | 当时跑的是什么版本？结果能否复现？ |
| 路线判断 | 已证实的结论、尚未验证的猜想、采用或暂缓的理由 | 为什么继续这条路线，或为什么停下来？ |

文档要区分**观察到什么**和**能得出什么结论**。例如，[本项目科研过程复盘](./科研过程复盘_Codex与Git协同工作流.md)记录过：全局优化中某些残差下降，但最终基站定位误差未同步改善。记录时应把两句话一起保留，不能只写“优化后指标变好”。这既便于之后的 Codex 正确理解，也提醒研究者不要用局部指标替代最终目标。

不需要把每句聊天原样复制到文档。每完成一个重要阶段，整理一份简短、可核查的结论，并附上论文出处、实验配置或代码版本。推导和结果如果之后被修正，也要更新文档，保留变更原因。

## 三、以后怎样让 Codex 接着研究

新聊天、聊天分叉，或隔了很久继续旧聊天时，不要求 Codex 凭“印象”回忆。先明确要读哪些文件，再让它核对当前项目状态：

> 请先读取本路线的算法推导、实验记录和研究决策文档，并检查当前 Git 分支与提交。先复述已证实结论、未验证假设和本次要使用的评价指标；发现文档、代码或实验结果不一致时，先指出，不要直接修改算法。

核对后再继续推导、实现或设计实验。完成一轮工作后，把新的结论、失败原因和对应版本写回文档。这样形成一个可重复的工作习惯：**读旧记录、核对证据、推进问题、更新记录**。

这种做法有助于降低长对话细节遗漏造成的误判，但不能保证 Codex 永远正确。文档可能过时，模型也可能读错；关键结论仍需回到原论文、代码和原始实验结果上验证。`AGENTS.md` 可以规定“开始任务先读哪些研究文档”，但具体推导与实验结论应写在专题文档中，而不是只留在聊天或规则文件里。

## 四、3 页 PPT 怎么讲

建议把这 3 页放在现有汇报的 Codex／VS Code 使用介绍之后、Git 章节之前。仍沿用白底、深灰标题、细分隔线的版式，不增加独立封面或装饰性 AI 图。三页只讲一条线：**长聊天有边界，科研成果写进文档，下次读取文档继续。**

### 第 1 页：长对话的上下文边界

- 页面问题：为什么同一段科研聊天聊久以后，不能只依赖早期讨论？
- 页面文字：`科研讨论会持续很久`；`当前可用上下文有限`；`压缩保留关键状态，但不逐字保留所有细节`。
- 版式：用一条从“论文讨论”延伸到“算法修改、实验、再讨论”的时间线表示聊天变长；时间线下方放一行风险提示。不要写具体 token 数或声称“压缩后一定忘记”。
- 讲解句：有些重要前提可能仍在聊天记录里，但未必能在后续回答中被准确使用。

### 第 2 页：把什么内容写成文档

- 页面问题：哪些科研成果不能只停留在聊天里？
- 页面文字分三类：`算法推导与适用前提`、`实验设置与结果`、`研究判断及其依据`。
- 版式：三条简洁的文字行，下面放本项目的真实例子：“某些残差下降”与“基站定位误差未同步改善”并列呈现。重点突出记录结论时不能丢掉限定条件。
- 讲解句：文档保存的是经过核对的结论及证据，不是聊天记录的简单复制。

### 第 3 页：下一轮研究怎样接上

- 页面问题：以后再谈同一算法时，Codex 具体怎么利用这些材料？
- 页面文字按顺序写：`读取相关文档`、`核对当前代码与实验配置`、`复述已证实和未证实的内容`、`继续实验并更新记录`。
- 版式：四步纵向流程，底部留一句结论：“文档让研究可以接续；代码和实验结果负责验证。”这页也自然引出后面的 Git 版本管理。
- 讲解句：每次开始都先核对证据，每次结束都把新的结果补回去；这样换聊天或长时间以后继续，也有明确的依据。

如果最终只能留 2 页，可以合并第 1、2 页；但推荐 3 页，因为科研案例和实际操作都值得单独讲清楚。
