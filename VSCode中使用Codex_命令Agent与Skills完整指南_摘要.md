# VSCode 中使用 Codex：命令、Agent 与 Skills 完整指南

> 本文档是原网页的结构化摘要，并非逐字转载。需要查看完整示例与原始表述时，请访问原文。

- 原文：[技术栈文章](https://jishuzhan.net/article/2053301309797892098)
- 作者：且去填词
- 发布日期：2026-05-10
- 整理日期：2026-09-21

## 核心内容

文章面向 Windows 与 VSCode 用户，建议以 Codex IDE 扩展作为日常入口，以终端和 Codex CLI 作为补充。推荐工作流是：打开完整项目、先做只读分析、确认修改方案、限定改动范围、检查差异、运行测试并总结结果。

文中将几类配置的职责区分为：

- `AGENTS.md`：记录项目级长期规则、技术栈、风险边界和交付要求。
- Skills：沉淀代码审查、数据库迁移、危险删除检查等可复用流程。
- Custom Agents：定义项目探索、代码审查、数据库安全等专门角色。
- Subagents：让多个角色并行分析复杂任务，再由主 Agent 汇总。
- `.codex/config.toml`：设置审批策略、沙箱模式和 Agent 并发参数。

## 实用要点

- VSCode 中可把选中代码或整个文件加入任务上下文，也可直接实现选中的 TODO。
- CLI 可用于查看版本、启动只读或工作区可写会话，以及执行一次性分析。
- 项目级 Skill 通常放在 `.agents/skills/`，自定义 Agent 放在 `.codex/agents/`。
- 数据库变更应先生成备份、迁移、回滚和验证方案，不宜直接执行。
- 删除文件或数据时，应设置范围预览、二次确认、日志和失败恢复机制。
- 长任务应适时压缩上下文或新建任务，避免历史信息干扰当前判断。

## 文章章节索引

1. 适用读者与推荐使用方式
2. VSCode 扩展安装、命令与新手流程
3. Codex CLI 与 Slash Commands
4. `AGENTS.md` 的用途、位置和示例
5. Skills 的目录结构及三类示例
6. Custom Agents、Subagents 与并行审查
7. `config.toml` 配置和推荐项目结构
8. 常用提示词模板、初学者注意事项与最终建议

## 一句话结论

先用规则明确边界，再把重复流程固化为 Skill，按需分配专业 Agent，最后让 Codex 在可审查、可验证、可回滚的范围内修改项目。
